# 動作仕様

1. `!join` を実行すると、Bot は実行者が入っている VC へ参加します。
2. 話し始めを検知すると、文字起こし用のメッセージを投稿します。
3. 話している間は、同じメッセージを編集して認識結果を更新します。
4. 話し終わると、そのメッセージを確定した内容に更新します。
5. 次に話し始めたときは、新しいメッセージを投稿します。
6. VC に Bot 以外がいなくなった場合は、自動で退出します。
